databaseChangeLog:
  - changeSet:
      id: 002-create-questions
      author: Konstantin Dobrenkii
      changes:
        - createSequence:
            sequenceName: questions_id_sequence
            startValue: 1
            incrementBy: 50
            cacheSize: 50
        - createTable:
            tableName: questions
            columns:
              - column:
                  name: id
                  type: BIGINT
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: key
                  type: VARCHAR(100)
                  constraints:
                    nullable: false
                    unique: true
              - column:
                  name: prompt
                  type: text
                  constraints:
                    nullable: false
              - column:
                  name: difficulty
                  type: VARCHAR(20)
                  constraints:
                    nullable: false
              - column:
                  name: topic_id
                  type: BIGINT
                  constraints:
                    nullable: false
              - column:
                  name: created_at
                  type: TIMESTAMPTZ
                  defaultValueComputed: now()
                  constraints:
                    nullable: false
              - column:
                  name: updated_at
                  type: TIMESTAMPTZ
                  defaultValueComputed: now()
                  constraints:
                    nullable: false

        - addForeignKeyConstraint:
            baseTableName: questions
            baseColumnNames: topic_id
            referencedTableName: topics
            referencedColumnNames: id
            onDelete: CASCADE
            constraintName: fk_questions_topic
# SQL:
# CREATE TABLE questions (
#   id UUID PRIMARY KEY,
#   key VARCHAR(100) NOT NULL UNIQUE,
#   prompt TEXT NOT NULL,
#   difficulty VARCHAR(20) NOT NULL,
#   topic_id UUID NOT NULL,
#   created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
#   updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
#   CONSTRAINT fk_questions_topic
#     FOREIGN KEY (topic_id) REFERENCES topics(id)
# );